export const RUN_TUNING = {
  survivalGoalSeconds: 120,
  initialSpawnDelayMs: 6000,
  baseSpawnIntervalMs: 10000,
  minimumSpawnIntervalMs: 7000,
  maximumSpawnIntervalMs: 14000,
  spawnIntervalChangeMs: 500,
  maxActiveEnemies: 4,
} as const;